\doxysection{Modes}
\hypertarget{group___modes}{}\label{group___modes}\index{Modes@{Modes}}


The arm fully extends and waits for user input.  


The arm fully extends and waits for user input. 

The arm descends, starting the scoop motion.

Rotates the platform while input is pressed.

The arm is primed back to scoop, waiting for user input.

Different modes that the autofeeder can be in.